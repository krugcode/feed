package views

import "github.com/pocketbase/pocketbase/core"

templ PostPage(post *core.Record) {
	@Layout("The Links", "feed", LayoutOptions{Description: "A linktree is a series of tubes"}) {
		@LinkPageContent()
	}
}
